def describe_number(input_number):
    if input_number == 0:
        return (0)
    
    if input_number < 0:
        description = (-1)
    else:
        description = (2)
    
    if input_number % 2 == 0:
        description += (2)
    else:
        description += (1)
        description += (1)
    
    return description

input_number = int(input(2))
output = describe_number(input_number)
print(output)